# =========================
# SHAPES
# =========================
scene = {
    width = 6000
    height = 3000
    brightness = 0.9
    max_depth = 4

    #here we will eventually put the camera settings
    #this will be done in issue #10
    #camera = (
    #   origin: point
    #   direction:vector
    #   zoom: double
    #)
}
shapes = (

    # --- Center sphere ---
    {
        type = "sphere"
        origin = [0, 0, -1]
        radius = 0.5
        color = "red"
    },

    # --- Left sphere ---
    {
        type = "sphere"
        origin = [-1.0, 0.0, -1.5]
        radius = 0.5
        color = "blue"
    },

    # --- Right sphere ---
    {
        type = "sphere"
        origin = [1.0, 0.0, -1.5]
        radius = 0.5
        color = "green"
    },

    # --- Small sphere ---
    {
        type = "sphere"
        origin = [0.3, -0.3, -0.5]
        radius = 0.2
        color = "yellow"
    },

    {
        type = "cylinder"
        origin = [-1.5, -1.0, -1.0]
        axis = [0.0, 1.0, 0.0]
        radius = 0.3
        height = 1.0
        color = "cyan"
    },

    # --- Ground plane ---
    {
        type = "plane"
        normal = [0, 1, 0]
        point = [0.0, -0.5, 0.0]
        color = "white"
        reflectivity = 0.15
    }
)

# =========================
# LIGHTS
# =========================
lights = (

    {
        type = "directional"
        direction = [-1, -1, -1]
        brightness = 0.8
        color_vector = [255, 255, 255]
    },

    {
        type = "specular"
        direction = [-1, -1, -1]
        brightness = 1.0
        shininess = 128.0
        specular_strength = 1.0
    },

    {
        type = "ambient"
        brightness = 0.8
        color_vector = [255, 255, 255]
    }
)