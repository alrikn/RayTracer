# =========================
# SHAPES
# =========================
shapes = (

    # --- Center sphere ---
    {
        type = "sphere";
        origin = [0, 0, -1];
        radius = 0.5;
        color = "red";
    },

    # --- Left sphere ---
    {
        type = "sphere";
        origin = [-1.0, 0.0, -1.5];
        radius = 0.5;
        color = "blue";
    },

    # --- Right sphere ---
    {
        type = "sphere";
        origin = [1.0, 0.0, -1.5];
        radius = 0.5;
        color = "green";
    },

    # --- Small sphere ---
    {
        type = "sphere";
        origin = [0.3, -0.3, -0.5];
        radius = 0.2;
        color = "yellow";
    },

    # --- Ground plane ---
    {
        type = "plane";
        normal = [0, 1, 0];
        point = [0.0, -0.5, 0.0];
        color = "white";
    }
);

# =========================
# LIGHTS
# =========================
lights = (

    {
        type = "directional";
        direction = [-1, -1, -1];
        brightness = 0.8;
    },

    {
        type = "specular";
        direction = [-1, -1, -1];
        brightness = 1.0;
        shininess = 128.0;
        specular_strength = 1.0;
    },

    {
        type = "ambient";
        brightness = 0.8;
    }
);